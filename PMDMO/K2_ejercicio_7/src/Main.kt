fun main(){
    var userName: String? = null
    val defaultName:String= "Guest"
    // var finalName: String = userName

    // El compilador de Kotlin detiene esta asignación debido a que finalName no puede contener el valor null
    // sin embargo, userName si que cuenta con la posibilidad de que lo sea


}